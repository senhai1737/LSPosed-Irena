# Master Log Switch 实现文档

## 概述
本实现为LSPosed-Irena添加了一个"master log switch"（日志总开关），可以全局控制所有日志功能。

## 修改的文件

### 1. ConfigManager.java (`/daemon/src/main/java/org/lsposed/lspd/service/ConfigManager.java`)
- **修改了 `setLogEnabled(boolean on)` 方法**:
  - 当日志总开关启用时，启动LogcatService
  - 当日志总开关关闭时，停止LogcatService并自动关闭详细日志
- **修改了 `setVerboseLog(boolean on)` 方法**:
  - 添加了日志总开关的检查：当日志总开关关闭时，不能启用详细日志
  - 启用详细日志时，确保日志服务已启动

### 2. LogcatService.java (`/daemon/src/main/java/org/lsposed/lspd/service/LogcatService.java`)
- **添加了 `stop()` 方法**:
  - 安全地停止日志服务线程
- **修改了 `start()` 方法**:
  - 在启动日志服务前检查日志总开关是否启用
- **修改了构造函数**:
  - 初始化时检查日志总开关，如果启用则自动启动日志服务
- **添加了ConfigManager导入**

## 功能特性

1. **层级控制**:
   - 日志总开关 (`enable_log`) 控制所有日志功能
   - 详细日志开关 (`disable_verbose_log`) 依赖于日志总开关
   - 当日志总开关关闭时，详细日志自动关闭且无法启用

2. **自动管理**:
   - 当日志总开关启用时，自动启动日志服务
   - 当日志总开关关闭时，自动停止日志服务
   - 系统初始化时根据配置自动启动/停止日志服务

3. **用户界面**:
   - SettingsFragment中已经实现了依赖关系
   - 当日志总开关关闭时，详细日志开关被禁用
   - 用户界面与后端逻辑保持一致

## 配置关系

```
日志总开关 (enable_log)
    ├── 启用: 启动日志服务，允许控制详细日志
    │    └── 详细日志开关 (disable_verbose_log)
    │        ├── 启用: 记录详细日志
    │        └── 禁用: 只记录模块日志
    └── 禁用: 停止所有日志服务，详细日志自动禁用且不可用
```

## 测试要点

1. **基本功能测试**:
   - 启用日志总开关 → 日志服务应启动
   - 关闭日志总开关 → 日志服务应停止，详细日志应自动关闭
   - 尝试在日志总开关关闭时启用详细日志 → 应被拒绝

2. **依赖关系测试**:
   - 当日志总开关关闭时，详细日志开关应在UI中被禁用
   - 当日志总开关启用时，详细日志开关应根据配置启用或禁用

3. **持久化测试**:
   - 重启系统后，日志设置应保持
   - 配置变更应正确保存到数据库

## 注意事项

1. **调试版本**: 在调试版本中 (`BuildConfig.DEBUG`)，详细日志控制可能被绕过
2. **线程安全**: 日志服务的启动和停止是线程安全的
3. **错误处理**: 添加了适当的错误日志和异常处理

## 文件备份
修改前已创建备份文件:
- `ConfigManager.java.bak`
- `LogcatService.java.bak`

如需恢复原始版本，可使用备份文件替换修改后的文件。
## 补充修改

### 3. 修改了 `verboseLog()` 方法
- **添加了日志总开关检查**:
  - 当日志总开关关闭时，`verboseLog()` 方法返回 `false`
  - 确保其他组件在查询详细日志状态时得到正确的结果
  - 即使 `verboseLog` 变量为 `true`，如果日志总开关关闭，也返回 `false`

### 4. 配置读取逻辑
在 `updateConfig()` 方法中，日志总开关的配置读取已经存在：
```java
Object bool = config.get("enable_log");  // 新增读取日志总开关配置
logEnabled = bool == null || (boolean) bool;
```

## 完整的工作流程

1. **系统启动**:
   - ConfigManager初始化，读取配置
   - LogcatService初始化，检查日志总开关
   - 如果日志总开关启用，启动日志服务

2. **用户操作**:
   - 用户启用/禁用日志总开关 → `setLogEnabled()` 被调用
   - 日志服务相应启动/停止
   - 详细日志状态自动同步

3. **查询状态**:
   - `isLogEnabled()` 返回日志总开关状态
   - `verboseLog()` 返回详细日志状态（受日志总开关影响）
   - UI组件根据这些状态更新界面

## 代码质量保证

1. **错误处理**: 所有关键操作都有错误日志
2. **线程安全**: 使用同步机制保护共享状态
3. **资源管理**: 正确启动和停止服务线程
4. **配置一致性**: 确保UI状态与后端状态一致